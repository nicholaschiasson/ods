name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-22.04

    steps:
    - uses: actions/checkout@v3
    - uses: awalsh128/cache-apt-pkgs-action@v1.3.1
      with:
        packages: inkscape=1.1.2-3ubuntu1 gnuplot=5.4.2+dfsg2-2 ipe=7.2.24+dfsg1-1build1
        version: 1.0
    - name: make
      run: make
